//----- spread operator----------//

// Spread operator allows an iterable to expand in the situations where more than one parameters are required, so that the iterable can provide the elements as the paramter.

let array = ['a', 'b', 'c'];

let arr2 = [...array];

console.log(arr2)